let initialState={
    username:'',
    id:'',
    profilePic:''
}

export default function reducer(state, action){
    switch (action.type) {
        // case DEFAULT:
        default:
            return state;
    }
}